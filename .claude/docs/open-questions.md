# Open Questions Log

Running list of questions and their answers as they get resolved. Add new ones at the top.

## Answered
- **Q: Max clock frequency on GF26a (out of the box and externally)?**
  A: The clock is generated by the board's RP2040, configurable **1 Hz – ~66.5 MHz** (docs cite
  ~66 MHz max). There isn't a separate "external" path beyond what the RP2040 drives onto `clk`;
  an external clock can be mapped to a GPIO per the FAQ
  (<https://tinytapeout.com/faq/#how-can-i-map-an-additional-external-clock-to-one-of-the-gpios>).
  The old design assumed 50 MHz → ~97.5 kHz switching; switching freq scales with `clk`.

## Open
- What final `clk` frequency to target on GF26a, and the resulting switching frequency / `clock_hz`?
- Re-add selectable PWM bit-width (5/6/7/8/9) and/or dithering modes from the VHDL, or keep the
  simplified fixed-8-bit normal/dyadic port? **Answer**: Selectible bit-width and dithering modes should be implemented as long as they fit into the 1x1 tile, but the final decision will depend on the pin budget and timing constraints.
- Pin budget / mapping for the future two-wire data modulator+demodulator alongside the PWM core. **Answer**: Right now, make use for Dyadic PWM as needed, if there are left, then we will add two-wire later, not necessary to think about it now.
- Any GF180MCU-specific timing or cell constraints that affect the async-reset style or dead-time?
- How much we can fit reliably into the 1×1 GF26a tile ?
