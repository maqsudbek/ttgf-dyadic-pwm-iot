
- Make this .claude folder and its content more correct, fetch, search info, look through all the docs and files and folders of the project here, and also online, and plan thoroughly how to make this .claude folder `/home/orangepi/ttgf-dyadic-pwm-iot/.claude` more correct, organized, and make token efficient, and ask questions if you need further clarifications, because some docs and info maybe vague. at the end make the plan to change this claude folder, to prepare it for the real development. change only files inside `.claude` folder, do not change anything from the project files.
After plan, i will decide to implement that, and when implementation finishes, you should generate the next prompt i should put in new chat with its all configs (model name, effort level, thinking on or off, mode and etc...) as handoff for the next chat, and also save that prompt in `.claude/prompts` folder as md file for future reference. also save the plan in `.claude/plans` folder as md file for future reference. and then we will start a new chat session with that prompt and continue the work.